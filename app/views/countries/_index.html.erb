<ul class="nav navbar-nav">
  <% @countries.each do |country| %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= country.name %> <b class="caret"></b></a>
        <!-- <ul>
        <% country.programs.each do |program| %>
        <li><%= program.name %></li>
        <% end %>
        </ul> -->
        <%= country.id %>
        <% programs = Program.where(country_id: country.id) %>
        <% if programs %>
          <% programs.each do |program| %>
            <%= program.name %>
          <% end %>
        <% end %>

        <%# render "programs/index" locals: { country_id: country_id } %>
      </li>
  <% end %>
</ul>